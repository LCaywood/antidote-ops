---
  name: "wait_until_pubtime"
  runner_type: "python-script"
  description: "Pauses until the next publishing date/time"
  enabled: true
  entry_point: "wait_until_pubtime.py"
  parameters:
    futuretime:
      type: "string"
      description: "Datetime (RFC3339) to wait until. Default is the next instance of 1700 UTC"
      required: false
      position: 0